"use client";
import { Bell } from "lucide-react";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import type { AgentNotificationDefinitions } from "@/types/agent-settings";
const events=[{id:"reservation_confirmed",name:"Reserva confirmada",help:"Envía una confirmación al cliente."},{id:"reservation_reminder",name:"Recordatorio de reserva",help:"Recuerda al cliente su próxima reserva."},{id:"payment_confirmed",name:"Pago confirmado",help:"Confirma que el pago fue recibido."}];
export function NotificationTemplateRules({templates,value,onChange}:{templates:string[];value:AgentNotificationDefinitions;onChange:(v:AgentNotificationDefinitions)=>void}){return <Card><CardHeader><div className="flex items-center gap-2"><Bell className="h-5 w-5 text-primary"/><CardTitle>Notificaciones y automatizaciones</CardTitle></div><CardDescription>Conecta cada evento con una plantilla de la biblioteca. Así el mensaje siempre tiene vista previa y un único origen.</CardDescription></CardHeader><CardContent className="space-y-3">{events.map(event=>{const rule=value[event.id]??{};const delivery=rule.deliveries?.[0]??{id:"customer",enabled:true,recipients:["source:conversation"],sendMessageSequence:null};return <div key={event.id} className="grid gap-3 rounded-xl border p-4 sm:grid-cols-[1fr_auto_220px]"><div><p className="font-medium">{event.name}</p><p className="text-sm text-muted-foreground">{event.help}</p></div><label className="flex items-center gap-2 text-sm"><Checkbox checked={rule.enabled===true} onCheckedChange={checked=>onChange({...value,[event.id]:{...rule,enabled:checked===true,deliveries:[delivery]}})}/>Activo</label><Select value={delivery.sendMessageSequence??"none"} onValueChange={sendMessageSequence=>onChange({...value,[event.id]:{...rule,deliveries:[{...delivery,sendMessageSequence:sendMessageSequence==="none"?null:sendMessageSequence}]}})}><SelectTrigger><SelectValue placeholder="Elegir plantilla"/></SelectTrigger><SelectContent><SelectItem value="none">Sin mensaje</SelectItem>{templates.map(template=><SelectItem key={template} value={template}>{template}</SelectItem>)}</SelectContent></Select></div>})}{templates.length===0&&<p className="rounded-lg bg-amber-50 p-3 text-sm text-amber-800">Crea al menos una plantilla para poder usarla en notificaciones.</p>}</CardContent></Card>}
